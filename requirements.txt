# FedWatch AI — Python 3.11
# 安装方式：pip install -r requirements.txt
# 开发环境：conda env fed-agent，2026-06-21 验证版本

# 核心工具
python-dotenv>=1.0.0
pyyaml>=6.0.0
requests>=2.28.0

# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# 计量模型
statsmodels>=0.14.0

# LLM / 多智能体（V1 流水线）
openai>=1.50.0
crewai>=0.80.0
crewai-tools>=0.8.0

# 数据抓取（历史声明爬取）
beautifulsoup4>=4.12.0
lxml>=5.0.0

# 可视化仪表盘
streamlit>=1.30.0
